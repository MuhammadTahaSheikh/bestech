# CMS uploads (team photos, blog covers) — served from site root, not under /api/.
Options -Indexes

<IfModule mod_authz_core.c>
  <FilesMatch "\.(jpg|jpeg|png|gif|webp|avif)$">
    Require all granted
  </FilesMatch>
</IfModule>
